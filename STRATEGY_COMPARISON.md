# Backpack 永续合约策略对比

## 策略概述

项目中有两个永续合约策略：

1. **永续做市策略** (`PerpetualMarketMaker`)
2. **Maker-Taker 对冲策略** (`MakerTakerHedgeStrategy`)

---

## 核心区别总结

| 特性 | 永续做市策略 | Maker-Taker 对冲策略 |
|------|-------------|---------------------|
| **目标持仓** | 允许持有净持仓（可做多或做空） | **保持接近0持仓（Delta Neutral）** |
| **订单数量** | 可设置多个挂单（max_orders） | **仅买一/卖一各1单** |
| **成交后行为** | 不立即对冲，保持持仓 | **每次成交立即市价对冲** |
| **风险暴露** | 有方向性风险 | 无方向性风险（对冲后） |
| **盈利来源** | 价差收益 + 方向性收益 | **仅价差收益** |
| **止损止盈** | 支持设置止损/止盈 | 不需要（已对冲） |
| **适用场景** | 看好方向 + 赚价差 | 纯做市，无方向判断 |

---

## 详细对比

### 1. 永续做市策略 (PerpetualMarketMaker)

**代码位置**: `strategies/perp_market_maker.py`

#### 工作原理

```
1. 在买一/卖一附近挂出限价单
   买单: bid - spread
   卖单: ask + spread

2. 订单成交后，保持持仓
   ✅ 允许持有净持仓

3. 持仓管理
   - 目标持仓: target_position（例如 1.0 SOL）
   - 最大持仓: max_position（例如 5.0 SOL）
   - 超过阈值时调整订单倾斜度

4. 风险控制
   - 止损: stop_loss（例如 -100 USDC）
   - 止盈: take_profit（例如 +200 USDC）
   - 未实现盈亏达到阈值时自动平仓
```

#### 关键参数

```python
PerpetualMarketMaker(
    api_key=api_key,
    secret_key=secret_key,
    symbol="SOL_USDC_PERP",
    base_spread_percentage=0.01,    # 价差 1%
    order_quantity=0.1,              # 每单数量
    max_orders=3,                    # 最多挂3对订单

    # 永续合约特有参数
    target_position=1.0,             # 目标持仓 1 SOL
    max_position=5.0,                # 最大持仓 5 SOL
    position_threshold=2.0,          # 触发调整阈值
    inventory_skew=0.002,            # 库存偏移 0.2%

    # 止损止盈
    stop_loss=100,                   # 止损 -100 USDC
    take_profit=200,                 # 止盈 +200 USDC
)
```

#### 实际运行示例

```
时刻 T0: 当前价格 100 USDC
  挂买单: 99.9 USDC, 0.1 SOL
  挂卖单: 100.1 USDC, 0.1 SOL

时刻 T1: 买单成交
  持仓: +0.1 SOL (做多)
  继续挂新订单（保持持仓）

时刻 T2: 价格上涨到 102 USDC
  未实现盈亏: (102 - 100) * 0.1 = +0.2 USDC
  继续持有

时刻 T3: 卖单成交
  持仓: 0 SOL (回到平仓)

时刻 T4: 价格下跌到 95 USDC，买单成交
  持仓: +0.1 SOL
  未实现盈亏: (95 - 95) * 0.1 = 0

时刻 T5: 价格继续下跌到 85 USDC
  未实现盈亏: (85 - 95) * 0.1 = -1.0 USDC
  ❌ 方向性风险暴露！
```

#### 优势

✅ **赚取价差 + 方向性收益**
- 如果价格朝有利方向移动，可以获得额外收益
- 适合有方向判断的交易员

✅ **资金利用率高**
- 不需要频繁对冲，减少手续费

✅ **灵活的持仓管理**
- 可根据市场情况调整目标持仓

#### 劣势

❌ **方向性风险**
- 价格剧烈波动时可能产生较大浮亏
- 需要设置止损保护

❌ **需要监控**
- 必须密切关注未实现盈亏
- 止损/止盈触发时可能影响策略连续性

---

### 2. Maker-Taker 对冲策略 (MakerTakerHedgeStrategy)

**代码位置**: `strategies/maker_taker_hedge.py`

#### 工作原理

```
1. 在买一/卖一位置挂出Post-Only订单
   买单: bid（买一价）
   卖单: ask（卖一价）

2. 订单成交后，立即市价对冲
   Maker成交买单 → 立即市价卖出（Taker）
   Maker成交卖单 → 立即市价买入（Taker）

3. 保持持仓接近0
   ✅ Delta Neutral（无方向性风险）

4. 盈利来源
   Maker Rebate（手续费返还）
   - Taker Fee（支付手续费）
   = 净收益
```

#### 关键代码

```python
# strategies/maker_taker_hedge.py:157-207

def _after_fill_processed(self, fill_info: Dict[str, Any]) -> None:
    """所有成交后立即以市价对冲。"""

    super()._after_fill_processed(fill_info)

    # 获取成交详情
    side = fill_info.get("side")
    quantity = float(fill_info.get("quantity", 0))

    # 立即提交反向市价单对冲
    hedge_side = "Ask" if side == "Bid" else "Bid"

    # 提交对冲订单
    order = {
        "orderType": "Market",
        "quantity": str(hedge_qty),
        "side": hedge_side,
        "symbol": self.symbol,
        "reduceOnly": True  # 确保是对冲订单
    }

    result = self.client.execute_order(order)
    # ...
```

#### 实际运行示例

```
时刻 T0: 当前价格 100 USDC
  挂买单: 99.99 USDC（买一价）, 0.1 SOL
  挂卖单: 100.01 USDC（卖一价）, 0.1 SOL

时刻 T1: 买单成交（Maker）
  成交: +0.1 SOL @ 99.99 USDC
  手续费: -0.001 USDC (Maker Rebate: -0.01%)
  持仓: +0.1 SOL

  ⚡ 立即对冲:
  市价卖单（Taker）: -0.1 SOL @ 100.00 USDC
  手续费: +0.006 USDC (Taker Fee: +0.06%)
  持仓: 0 SOL ✅

收益计算:
  买入成本: 99.99 * 0.1 = 9.999 USDC
  卖出收入: 100.00 * 0.1 = 10.000 USDC
  Maker Rebate: -0.001 USDC (返还)
  Taker Fee: +0.006 USDC (支付)
  净收益: 10.000 - 9.999 - 0.006 - (-0.001) = -0.004 USDC

时刻 T2: 卖单成交（Maker）
  成交: -0.1 SOL @ 100.01 USDC
  手续费: -0.001 USDC (Maker Rebate)
  持仓: -0.1 SOL

  ⚡ 立即对冲:
  市价买单（Taker）: +0.1 SOL @ 100.00 USDC
  手续费: +0.006 USDC (Taker Fee)
  持仓: 0 SOL ✅

收益计算:
  卖出收入: 100.01 * 0.1 = 10.001 USDC
  买入成本: 100.00 * 0.1 = 10.000 USDC
  Maker Rebate: -0.001 USDC
  Taker Fee: +0.006 USDC
  净收益: 10.001 - 10.000 - 0.006 - (-0.001) = -0.004 USDC
```

#### 关键特性

**1. 残量累积机制**

```python
# 如果对冲量小于最小下单量，累积到下次
if hedge_qty < self.min_order_size:
    self._hedge_residuals[hedge_side] = target_quantity
    logger.info("对冲目标低于最小下单量，累积至下次")
    return
```

**2. 多次重试对冲**

```python
# 最多尝试3次对冲，确保持仓归零
for attempt in range(1, 4):
    if remaining_quantity < self.min_order_size:
        break

    # 提交市价对冲单
    result = self.client.execute_order(order)
    # ...
```

**3. 仓位参考跟踪**

```python
# 维护参考仓位，确保对冲后归零
self._hedge_position_reference = 0.0
current_position = self._fetch_current_position_reference()

if abs(current_position) > self._hedge_flat_tolerance:
    # 执行对冲
```

#### 优势

✅ **无方向性风险**
- 每次成交后立即对冲，持仓始终接近0
- 不受价格波动影响

✅ **稳定盈利**
- 只要 Maker Rebate > Taker Fee，就能盈利
- 收益可预测，波动小

✅ **无需监控**
- 不需要设置止损/止盈
- 自动对冲，无需人工干预

✅ **适合所有市场**
- 牛市、熊市、震荡市都适用
- 纯做市，无需判断方向

#### 劣势

❌ **手续费成本高**
- 每次成交需要支付一次 Maker + 一次 Taker 手续费
- 频繁对冲增加交易成本

❌ **依赖交易所手续费政策**
- Maker Rebate 必须足够大
- 如果手续费政策改变，策略可能不再盈利

❌ **滑点风险**
- 市价对冲可能遇到滑点
- 流动性不足时对冲成本增加

❌ **资金效率较低**
- 需要频繁对冲，占用资金

---

## 手续费对比

### Backpack 手续费结构

| 订单类型 | 手续费率 |
|---------|---------|
| **Maker** | -0.01% (返还) |
| **Taker** | +0.06% (支付) |

### 永续做市策略手续费

```
假设成交 1000 USDC:
  买入 Maker: -1000 * 0.01% = -0.10 USDC（返还）
  卖出 Maker: -1000 * 0.01% = -0.10 USDC（返还）
  总手续费: -0.20 USDC ✅ 净收入
```

### Maker-Taker 对冲策略手续费

```
假设成交 1000 USDC:
  买入 Maker: -1000 * 0.01% = -0.10 USDC（返还）
  卖出 Taker: +1000 * 0.06% = +0.60 USDC（支付）
  净手续费: +0.50 USDC ❌ 净支出

盈利条件:
  价差收益 > 0.50 USDC 才能盈利
```

---

## 使用场景建议

### 选择永续做市策略的情况

✅ **有方向判断能力**
- 看好市场方向，希望赚取价差 + 方向性收益

✅ **能接受风险**
- 可以承受持仓带来的浮亏风险
- 愿意设置止损保护

✅ **市场趋势明显**
- 牛市或熊市，方向性机会多

✅ **资金充足**
- 可以承受较大回撤

### 选择 Maker-Taker 对冲策略的情况

✅ **纯做市需求**
- 只想赚取价差，不想承担方向性风险

✅ **风险厌恶**
- 无法承受持仓波动
- 希望收益稳定可预测

✅ **震荡市场**
- 价格波动大，方向不明确
- 纯做市更安全

✅ **自动化交易**
- 无需人工监控
- 策略全自动运行

---

## 配置示例

### 永续做市策略配置

```bash
python run.py \
  --exchange backpack \
  --market-type perp \
  --symbol SOL_USDC_PERP \
  --strategy standard \
  --spread 0.01 \
  --quantity 0.1 \
  --max-orders 3 \
  --target-position 1.0 \
  --max-position 5.0 \
  --position-threshold 2.0 \
  --inventory-skew 0.002 \
  --stop-loss 100 \
  --take-profit 200 \
  --duration 3600 \
  --interval 60
```

### Maker-Taker 对冲策略配置

```bash
python run.py \
  --exchange backpack \
  --market-type perp \
  --symbol SOL_USDC_PERP \
  --strategy maker_hedge \
  --spread 0.01 \
  --quantity 0.1 \
  --duration 3600 \
  --interval 60
```

注意：Maker-Taker 策略不需要设置：
- `max_orders`（固定为1）
- `target_position`（固定为0）
- `stop_loss`/`take_profit`（不需要）

---

## 性能对比

### 假设市场条件

- SOL 价格: 100 USDC
- 订单数量: 0.1 SOL
- 运行时间: 24小时
- 成交次数: 100次（50买 + 50卖）

### 永续做市策略

```
收益来源:
  1. Maker Rebate: 100 * 10 USDC * 0.01% = 1.00 USDC
  2. 价差收益: 100 * 10 USDC * 0.02% = 2.00 USDC（假设价差0.02%）
  3. 方向性收益: ±5.00 USDC（取决于价格走势）

总收益: 1.00 + 2.00 ± 5.00 = -2.00 ~ +8.00 USDC
收益率: -0.02% ~ +0.08%
```

### Maker-Taker 对冲策略

```
收益来源:
  1. Maker Rebate: 100 * 10 USDC * 0.01% = 1.00 USDC
  2. Taker Fee: -100 * 10 USDC * 0.06% = -6.00 USDC
  3. 价差收益: 100 * 10 USDC * 0.02% = 2.00 USDC

总收益: 1.00 - 6.00 + 2.00 = -3.00 USDC ❌ 亏损
收益率: -0.03%

⚠️ 需要更大的价差才能盈利！
```

---

## 总结

| 指标 | 永续做市 | Maker-Taker 对冲 |
|-----|---------|-----------------|
| **风险程度** | ⚠️ 中-高 | ✅ 低 |
| **收益潜力** | 📈 高 | 📉 低 |
| **收益稳定性** | ❌ 波动大 | ✅ 稳定 |
| **手续费成本** | ✅ 低 | ❌ 高 |
| **技术要求** | 🔧 中等 | 🔧 低 |
| **监控需求** | 👁️ 需要 | ✅ 不需要 |
| **适用市场** | 📊 趋势市 | 📊 所有市场 |

**推荐**:
- 🎯 **新手**: Maker-Taker 对冲策略（安全稳定）
- 🎯 **有经验的交易员**: 永续做市策略（收益更高）
- 🎯 **震荡市**: Maker-Taker 对冲策略
- 🎯 **趋势市**: 永续做市策略

---

**版本**: 1.0
**最后更新**: 2025-11-14
**作者**: Claude Code
